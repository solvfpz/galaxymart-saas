version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: galaxymart-payments
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
